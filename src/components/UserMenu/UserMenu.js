const UserMenu = () => {
  return (
    <div>
      <p>Welcome, usrname</p>
      <button
        type="button"
        // onClick={() => dispatch(logOut())}
      >
        Logout
      </button>
    </div>
  );
};
export default UserMenu;
